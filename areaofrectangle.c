#include <stdio.h>
#include <conio.h>

void main()
{
    int l,b,AOR;
    printf("Enter the length : ");
    scanf("%d", &l);
    printf("Enter the bredth : ");
    scanf("%d", &b);
    AOR = l*b;
    printf("Area of the rectangle is %d", AOR);
    getch ();
}